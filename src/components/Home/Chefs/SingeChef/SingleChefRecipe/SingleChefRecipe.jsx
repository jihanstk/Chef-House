import React from "react";

const SingleChefRecipe = ({ recipe }) => {
  return (
    <div>
      {recipe.map((CR) => {
        <h1></h1>;
      })}
    </div>
  );
};

export default SingleChefRecipe;
